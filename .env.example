PORT=5177

# Optional. If set, the app uses the OpenAI Responses API for the final risk narrative.
# If omitted, the prototype still runs with a transparent local synthesis fallback.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-astra
